<div class="container my-5">
  <div class="px-5 py-3 my-3 bg-warning-subtle">
    <h1 class="text-center semibold">OK. What was your experience with <%= @restaurant.name %>? 🤔</h1>

    <%= simple_form_for [@restaurant, @review] do |f| %>
      <%= f.input :rating %>
      <%= f.input :content %>
      <%= f.submit("👉 Post your review", class: 'btn btn-success')  %>
    <% end %>
  </div>
  <div class="mt-3">
    <p><%= link_to "👈 Back to #{@restaurant.name}'s info", restaurant_path(@restaurant), class: 'btn btn-secondary' %></p>
  </div>

</div>
